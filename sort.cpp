#include <iostream>
#include <vector>
using namespace std;

void quickSort(vector<int> arr, int low, int high);
int partition(vector<int> arr, int low, int high);

void quickSort(vector<int> arr, int low, int high) {
    
        //quick sort function for the leaderboard
    
}

int partition(vector<int> arr, int low, int high) {
    //pivoting for sorting
 }
   
